// Stub for @icr/polyseg-wasm — we don't use polygon segmentation in this
// app, so an empty default export prevents Vite from trying to bundle the
// .wasm binary (which it can't do without ESM-WASM integration).
export default {};
